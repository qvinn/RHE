#Slave_server

- make slave_server_app(цель по-умолчанию - просто make) - сборка slave-сервера для PC
	-- При такой сбоке появится папка "build" -> запускать "sudo ./slave_server"
	-- .ini При отсутствии сгенерируется по умолчанию
	-- Отладчик НЕ доступен

- make slave_server_app HW_EN=YES - сборка slave-сервера для mini-PC(ПОКА ЧТО НЕ ИСПОЛЬЗОВАТЬ)
	-- При такой сбоке появится папка "build" -> запускать "sudo ./slave_server"
	-- .ini При отсутствии сгенерируется по умолчанию
	-- Отладчик доступен

- make debug_manager - Сборка менеджера отладки(ПОКА ЧТО НЕ ИСПОЛЬЗОВАТЬ)
